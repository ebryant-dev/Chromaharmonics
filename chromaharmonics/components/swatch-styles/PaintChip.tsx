import React from 'react';
import { SwatchRendererProps } from './types';

// TODO: Paint Chip style — each group rendered as a tall narrow swatch (Pantone/paint chip feel):
//   color fills most of the height, small white tab at the bottom with label + hex.
//   Variants fan out as offset chips beneath the primary.
const PaintChip: React.FC<SwatchRendererProps> = () => null;

export default PaintChip;
